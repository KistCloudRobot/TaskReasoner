
PLAN Conclude GoalFinished($goalName, $caller,$goalID,$goal) {
	ID : "GoalFinishedHandle"

	BODY :
		System.out.println("Goal received : " + $goalName);
		assert GoalComplete($goalName);	
		retract GoalFinished;
	UTILITY : 100000000;
}